import 'package:flutter/material.dart';
import 'operations_pages.dart';
import 'game_state.dart';
class NewCompanyPage extends StatefulWidget{final VoidCallback onStart;const NewCompanyPage({super.key,required this.onStart});@override State<NewCompanyPage> createState()=>_NewCompanyPageState();}
class _NewCompanyPageState extends State<NewCompanyPage>{String city='Dallas, TX',difficulty='Standard',strategy='Dry Van';final company=TextEditingController(text:'ProblemTechs Transport');@override void dispose(){company.dispose();super.dispose();}@override Widget build(BuildContext c)=>Scaffold(body:Center(child:SingleChildScrollView(child:ConstrainedBox(constraints:const BoxConstraints(maxWidth:720),child:Card(child:Padding(padding:const EdgeInsets.all(28),child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[const Row(children:[Icon(Icons.local_shipping,color:green,size:42),SizedBox(width:12),Text('TRUCK MANAGER',style:TextStyle(fontSize:30,fontWeight:FontWeight.w900))]),const SizedBox(height:8),const Text('PLAYER: ProblemTechs',style:TextStyle(color:green,fontWeight:FontWeight.bold)),const Text('START A NEW TRUCKING COMPANY',style:TextStyle(fontSize:20,fontWeight:FontWeight.bold)),const Text('Build a carrier from a small operation into a nationwide transportation company.',style:TextStyle(color:Colors.white60)),const SizedBox(height:24),TextField(controller:company,decoration:const InputDecoration(labelText:'Company name',border:OutlineInputBorder())),const SizedBox(height:14),DropdownButtonFormField(value:city,decoration:const InputDecoration(labelText:'Starting city',border:OutlineInputBorder()),items:['Dallas, TX','Atlanta, GA','Chicago, IL','Denver, CO','Phoenix, AZ','Los Angeles, CA','Newark, NJ'].map((x)=>DropdownMenuItem(value:x,child:Text(x))).toList(),onChanged:(v)=>setState(()=>city=v!)),const SizedBox(height:14),DropdownButtonFormField(value:difficulty,decoration:const InputDecoration(labelText:'Economy difficulty',border:OutlineInputBorder()),items:['Relaxed','Standard','Realistic'].map((x)=>DropdownMenuItem(value:x,child:Text(x))).toList(),onChanged:(v)=>setState(()=>difficulty=v!)),const SizedBox(height:14),DropdownButtonFormField(value:strategy,decoration:const InputDecoration(labelText:'Starting business',border:OutlineInputBorder()),items:['Cargo Van / Expedited','Box Truck / Local','Hotshot','Dry Van','Refrigerated'].map((x)=>DropdownMenuItem(value:x,child:Text(x))).toList(),onChanged:(v)=>setState(()=>strategy=v!)),const SizedBox(height:20),const Text('Version 1: Management Mode',style:TextStyle(color:green,fontWeight:FontWeight.bold)),const Text('You own and operate the company. Your hired drivers operate the equipment. Driver Mode is reserved for a future update.'),const SizedBox(height:24),SizedBox(width:double.infinity,child:FilledButton.icon(onPressed:(){truckGameState.companyName=company.text.trim().isEmpty?'ProblemTechs Transport':company.text.trim();widget.onStart();},icon:const Icon(Icons.business_center),label:const Padding(padding:EdgeInsets.all(14),child:Text('CREATE COMPANY'))))])))))));}}
class SettingsPage extends StatelessWidget{const SettingsPage({super.key});@override Widget build(BuildContext c)=>OpsPage('GAME SETTINGS','Interface, simulation and management preferences',[Box('Player & Company',Icons.account_circle,[row('Player','ProblemTechs'),row('Company',truckGameState.companyName),row('Company level','${truckGameState.companyLevel}'),row('Company XP','${truckGameState.companyXp}')]),Box('Starter Actions',Icons.play_circle,[Padding(padding:const EdgeInsets.all(8),child:Wrap(spacing:8,runSpacing:8,children:[FilledButton.icon(onPressed:()=>truckGameState.purchaseStarterTruck(),icon:const Icon(Icons.local_shipping),label:const Text('Buy Truck $125,000')),FilledButton.icon(onPressed:()=>truckGameState.hireDriver(),icon:const Icon(Icons.person_add),label:const Text('Hire Driver $1,000'))]))]),Box('Simulation',Icons.speed,[row('Default speed','1×'),row('Pause on critical events','Enabled'),row('Auto-save','Enabled'),row('Auto-save interval','5 minutes')]),Box('Management Automation',Icons.smart_toy,[row('Dispatch','Auto'),row('Load planning','Staff'),row('Team driver switching','Automatic'),row('Fuel stops','Automatic'),row('Routine compliance','Staff')])]);}
class NotificationsPage extends StatelessWidget{const NotificationsPage({super.key});@override Widget build(BuildContext c)=>OpsPage('OPERATIONS ALERTS','Central event and notification center',[Box('CRITICAL • Unit 207',Icons.warning,[row('Event','DOT out-of-service'),row('Cause','Brake defect'),row('Load risk','High'),row('Response','Repair / swap tractor')]),Box('IMPORTANT • Driver D-0240',Icons.medical_information,[row('Event','Medical card expiring'),row('Expires','Sep 28, 2026'),row('Days remaining','13'),row('Action','Schedule DOT physical')]),Box('ATTENTION • Maintenance',Icons.build_circle,[row('PM due','4 units'),row('Overdue','1 unit'),row('Shop capacity','1 bay available'),row('Roadside calls','1')]),Box('INFO • Contract',Icons.handshake,[row('Customer','Metro Retail Network'),row('Performance','98.2% on-time'),row('Target','97%'),row('Status','Above target')])]);}
